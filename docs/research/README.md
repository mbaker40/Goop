# Research outputs (PLAN §16.1)

Each file below is a placeholder for a dedicated research pass to run as a separate Claude session
before the milestone that needs it. Replace the stub with runnable findings; cite versions/dates.

- **`threejs.md`** — verify current three.js version + addon import paths; working `MarchingCubes`,
  `EffectComposer` bloom, and raymarched SDF metaball snippets; WebGL2 vs WebGPU status. **Needed for M1.**
- **`balance.md`** — incremental cost/prestige-curve conventions (Cookie Clicker 1.15, AdCap
  milestones) and pitfalls (dead zones, hard walls). Informs the harness tuning in `balance-notes.md`.
- **`juice.md`** — squash/stretch, screen-shake, particle, and audio-feedback techniques with
  parameter ranges. **Needed for M2/M5.**
- **`mobile.md`** — canvas sizing/DPR, touch-input gotchas, iOS Safari audio + memory limits.
  **Needed for M1 responsive + M2 audio.**
